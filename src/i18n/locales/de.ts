/**
 * de strings.
 *
 * Untranslated keys fall back to English by design (`enableFallback`), so a
 * partial file ships safely. `safety.*` keys must NOT be added here until a
 * clinical reviewer fluent in this language has signed them off — see
 * docs/SAFETY.md, "Localised safety copy".
 */
export default {} as const;
